doctype html
html
  head
    title Đọc HTML từ URL
  body
    h1 Đọc HTML từ URL
    form(action="/result", method="post")
      label(for="url") Nhập URL:
      input(type="text", name="url", id="url")
      button(type="submit") Submit
